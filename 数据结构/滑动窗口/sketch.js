function slideWindow(str) {
  let left = 0, right = 0;
  while(right < str.length) {
    // 即将进入窗口的字符
    const cur = str[right]
    // 增大窗口
    right++
    // 进行窗口内的一系列更新
    // ...
    // 判断窗口是否要收缩
    while(window needs shrink) {
      // 即将要移除窗口的字符
      const d = str[left]
      // 收缩窗口
      left++
      // 进行窗口内一系列数据的更新
    }
  }
}